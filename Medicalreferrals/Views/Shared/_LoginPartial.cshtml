@using Microsoft.AspNet.Identity
    @if (Request.IsAuthenticated)
    {using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm"})){@Html.AntiForgeryToken()@*@Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })*@<a href="javascript:document.getElementById(\'logoutForm\').submit()" id="logoutLink"><i class="fa fa-sign-out"></i> Ելք</a><a id="userLink"><i class="fa fa-user"></i> @User.Identity.GetUserName()</a>}
    }
    else
    {<a href="@Url.Action("Login", "Account")" id="loginLink"><i class="fa fa-sign-in"></i> Մուտք</a>}